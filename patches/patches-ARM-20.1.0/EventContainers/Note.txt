This is not completely fixed. Once patched it still takes 3 tries for it to build (without doing cmt config).
It may be quicker using -i option. The problem is that libraries are being built out of order.
